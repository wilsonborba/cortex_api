[Service]
# The workstation has one 8 GB GPU. Keep exactly one model in VRAM and let
# Ollama serialize generation instead of competing for memory.
Environment="OLLAMA_MAX_LOADED_MODELS=1"
Environment="OLLAMA_NUM_PARALLEL=1"
Environment="OLLAMA_MAX_QUEUE=4"
Environment="OLLAMA_KEEP_ALIVE=2m"
